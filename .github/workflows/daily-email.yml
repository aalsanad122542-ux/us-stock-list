name: Daily Stock Email

on:
  schedule:
    # Run 5 times daily at UTC times
    - cron: "0 4 * * *"    # 04:00 UTC
    - cron: "0 8 * * *"    # 08:00 UTC
    - cron: "0 12 * * *"   # 12:00 UTC
    - cron: "0 16 * * *"   # 16:00 UTC
    - cron: "0 20 * * *"   # 20:00 UTC
  workflow_dispatch:  # Manual trigger

jobs:
  send-email:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install google-generativeai python-dotenv schedule

      - name: Debug secrets
        run: |
          echo "GEMINI_API_KEY secret exists: ${{ secrets.GEMINI_API_KEY != '' }}"
          echo "SMTP_SERVER secret exists: ${{ secrets.SMTP_SERVER != '' }}"
          echo "SMTP_PORT secret exists: ${{ secrets.SMTP_PORT != '' }}"
          echo "SENDER_EMAIL secret exists: ${{ secrets.SENDER_EMAIL != '' }}"
          echo "RECIPIENT_EMAIL secret exists: ${{ secrets.RECIPIENT_EMAIL != '' }}"

      - name: Create .env file
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
        run: |
          echo "GEMINI_API_KEY=$GEMINI_API_KEY" > .env
          echo "SMTP_SERVER=$SMTP_SERVER" >> .env
          echo "SMTP_PORT=$SMTP_PORT" >> .env
          echo "SENDER_EMAIL=$SENDER_EMAIL" >> .env
          echo "SENDER_PASSWORD=$SENDER_PASSWORD" >> .env
          echo "RECIPIENT_EMAIL=$RECIPIENT_EMAIL" >> .env
          cat .env

      - name: Run daily analysis
        run: |
          python run_github_action.py 2>&1 | tee output.log

      - name: Upload output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: workflow-output
          path: output.log
          retention-days: 1
